// Code generated by spdmgen. DO NOT EDIT.

package msgs

import "github.com/xaionaro-go/spdm/pkg/gen/codes"

// PSKExchange is the SPDM PSK_EXCHANGE request per DSP0274 Section 10.14.
type PSKExchange struct {
	Header       MessageHeader
	ReqSessionID uint16
	PSKHintLen   uint16
	ContextLen   uint16
	OpaqueLen    uint16
	PSKHint      []byte
	Context      []byte
	OpaqueData   []byte
}

func (m *PSKExchange) RequestCode() codes.RequestCode { return codes.RequestPSKExchange }

func (m *PSKExchange) Marshal() ([]byte, error) {
	size := HeaderSize + 8 + len(m.PSKHint) + len(m.Context) + len(m.OpaqueData)
	buf := make([]byte, 0, size)
	hdr, _ := m.Header.Marshal()
	buf = append(buf, hdr...)
	b := [8]byte{}
	le.PutUint16(b[0:], m.ReqSessionID)
	le.PutUint16(b[2:], uint16(len(m.PSKHint)))
	le.PutUint16(b[4:], uint16(len(m.Context)))
	le.PutUint16(b[6:], uint16(len(m.OpaqueData)))
	buf = append(buf, b[:]...)
	buf = append(buf, m.PSKHint...)
	buf = append(buf, m.Context...)
	buf = append(buf, m.OpaqueData...)
	return buf, nil
}

func (m *PSKExchange) Unmarshal(data []byte) error {
	if len(data) < HeaderSize+8 {
		return ErrShortBuffer
	}
	if err := m.Header.Unmarshal(data); err != nil {
		return err
	}
	off := HeaderSize
	m.ReqSessionID = le.Uint16(data[off:])
	m.PSKHintLen = le.Uint16(data[off+2:])
	m.ContextLen = le.Uint16(data[off+4:])
	m.OpaqueLen = le.Uint16(data[off+6:])
	off += 8
	total := int(m.PSKHintLen) + int(m.ContextLen) + int(m.OpaqueLen)
	if off+total > len(data) {
		return ErrShortBuffer
	}
	m.PSKHint = make([]byte, m.PSKHintLen)
	copy(m.PSKHint, data[off:])
	off += int(m.PSKHintLen)
	m.Context = make([]byte, m.ContextLen)
	copy(m.Context, data[off:])
	off += int(m.ContextLen)
	m.OpaqueData = make([]byte, m.OpaqueLen)
	copy(m.OpaqueData, data[off:])
	return nil
}

// PSKExchangeResponse is the SPDM PSK_EXCHANGE_RSP response per DSP0274 Section 10.14.
type PSKExchangeResponse struct {
	Header                 MessageHeader
	RspSessionID           uint16
	Reserved               uint16
	ContextLen             uint16
	OpaqueLen              uint16
	MeasurementSummaryHash []byte
	Context                []byte
	OpaqueData             []byte
	VerifyData             []byte
}

func (m *PSKExchangeResponse) ResponseCode() codes.ResponseCode {
	return codes.ResponsePSKExchangeRsp
}

func (m *PSKExchangeResponse) Marshal() ([]byte, error) {
	size := HeaderSize + 8 + len(m.MeasurementSummaryHash) + len(m.Context) + len(m.OpaqueData) + len(m.VerifyData)
	buf := make([]byte, 0, size)
	hdr, _ := m.Header.Marshal()
	buf = append(buf, hdr...)
	b := [8]byte{}
	le.PutUint16(b[0:], m.RspSessionID)
	le.PutUint16(b[2:], m.Reserved)
	le.PutUint16(b[4:], uint16(len(m.Context)))
	le.PutUint16(b[6:], uint16(len(m.OpaqueData)))
	buf = append(buf, b[:]...)
	buf = append(buf, m.MeasurementSummaryHash...)
	buf = append(buf, m.Context...)
	buf = append(buf, m.OpaqueData...)
	buf = append(buf, m.VerifyData...)
	return buf, nil
}

func (m *PSKExchangeResponse) Unmarshal(data []byte) error {
	if len(data) < HeaderSize+8 {
		return ErrShortBuffer
	}
	if err := m.Header.Unmarshal(data); err != nil {
		return err
	}
	off := HeaderSize
	m.RspSessionID = le.Uint16(data[off:])
	m.Reserved = le.Uint16(data[off+2:])
	m.ContextLen = le.Uint16(data[off+4:])
	m.OpaqueLen = le.Uint16(data[off+6:])
	return nil
}

// UnmarshalWithSizes unmarshals the full PSK_EXCHANGE_RSP with negotiated sizes.
// hashSize is the size of MeasurementSummaryHash (0 when NoMeasurementSummaryHash).
// hmacSize is the size of the VerifyData HMAC at the end.
func (m *PSKExchangeResponse) UnmarshalWithSizes(
	data []byte,
	hashSize, hmacSize int,
) error {
	if len(data) < HeaderSize+8 {
		return ErrShortBuffer
	}
	if err := m.Header.Unmarshal(data); err != nil {
		return err
	}

	off := HeaderSize
	m.RspSessionID = le.Uint16(data[off:])
	m.Reserved = le.Uint16(data[off+2:])
	m.ContextLen = le.Uint16(data[off+4:])
	m.OpaqueLen = le.Uint16(data[off+6:])
	off += 8

	// MeasurementSummaryHash
	if off+hashSize > len(data) {
		return ErrShortBuffer
	}
	if hashSize > 0 {
		m.MeasurementSummaryHash = make([]byte, hashSize)
		copy(m.MeasurementSummaryHash, data[off:off+hashSize])
	}
	off += hashSize

	// Context
	ctxLen := int(m.ContextLen)
	if off+ctxLen > len(data) {
		return ErrShortBuffer
	}
	m.Context = make([]byte, ctxLen)
	copy(m.Context, data[off:off+ctxLen])
	off += ctxLen

	// OpaqueData
	opLen := int(m.OpaqueLen)
	if off+opLen > len(data) {
		return ErrShortBuffer
	}
	m.OpaqueData = make([]byte, opLen)
	copy(m.OpaqueData, data[off:off+opLen])
	off += opLen

	// VerifyData (HMAC)
	if off+hmacSize > len(data) {
		return ErrShortBuffer
	}
	m.VerifyData = make([]byte, hmacSize)
	copy(m.VerifyData, data[off:off+hmacSize])

	return nil
}

// PSKFinish is the SPDM PSK_FINISH request per DSP0274 Section 10.15.
type PSKFinish struct {
	Header     MessageHeader
	VerifyData []byte
}

func (m *PSKFinish) RequestCode() codes.RequestCode { return codes.RequestPSKFinish }

func (m *PSKFinish) Marshal() ([]byte, error) {
	buf := make([]byte, 0, HeaderSize+len(m.VerifyData))
	hdr, _ := m.Header.Marshal()
	buf = append(buf, hdr...)
	buf = append(buf, m.VerifyData...)
	return buf, nil
}

func (m *PSKFinish) Unmarshal(data []byte) error {
	return m.Header.Unmarshal(data)
}

// PSKFinishResponse is the SPDM PSK_FINISH_RSP response per DSP0274 Section 10.15.
type PSKFinishResponse struct {
	Header MessageHeader
}

func (m *PSKFinishResponse) ResponseCode() codes.ResponseCode { return codes.ResponsePSKFinishRsp }

func (m *PSKFinishResponse) Marshal() ([]byte, error) {
	return m.Header.Marshal()
}

func (m *PSKFinishResponse) Unmarshal(data []byte) error {
	return m.Header.Unmarshal(data)
}
